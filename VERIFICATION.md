# Verification status

Generated on 2026-09-11 for v0.5.0.

## Checks completed in this artifact environment

- `pubspec.yaml`, `analysis_options.yaml`, and `.github/workflows/flutter-ci.yml` parse as YAML.
- All non-generated relative Dart imports in `lib/` and `test/` resolve to existing files. `di_config.config.dart` is intentionally generated by `injectable_generator` during bootstrap.
- Raw brace-count smoke checks are balanced across Dart source/test files.
- `tool/bootstrap.sh` passes `bash -n` syntax validation.
- No embedded browser integration remains in the project; Mofid login is implemented through direct HTTP requests.
- No `.har` capture is present inside the project tree.
- A targeted scan found no literal captured Mofid/iPasargad JWT, Bearer token, session cookie, login name, or password from the HAR captures in project files.
- Mofid username/password are runtime inputs only; successful login persists them through `flutter_secure_storage` for biometric re-entry.
- Direct Mofid login code uses a fresh PKCE pair, manually follows HTTP redirects with an in-memory cookie jar, parses `__RequestVerificationToken`, posts `Username`/`Password`, validates callback `state`, then exchanges the authorization code at `/connect/token`.
- Native biometric integration is wired through `local_auth`; Android bootstrap adds `USE_BIOMETRIC`, changes `MainActivity` to `FlutterFragmentActivity`, sets minSdk 24, and changes `LaunchTheme` to an AppCompat theme.
- Unit tests were added for anti-forgery/form parsing, direct PKCE credential login, saved-credential re-login, and biometric Cubit behavior.
- The only source-visible password intentionally present is the LAN portfolio-sharing password `portfolio123`, paired with username `viewer`, per the requested product behavior. It is unrelated to investment-account credentials.
- The iPasargad adapter contains only public endpoint paths and the cookie name `otauth-FU`; no captured token or personal holding values are embedded.

## Checks that cannot be executed in this environment

The artifact environment does not have the Flutter or Dart SDK installed, so these commands have **not** been executed here:

```bash
dart format lib test
flutter analyze --no-fatal-infos
flutter test
flutter build apk --debug
```

Because Flutter is unavailable here, the newly added tests could not be observed in a red/green TDD run. The included GitHub Actions/Codex handoff performs bootstrap, formatting, analysis, tests, and a debug APK build in a Flutter-capable environment.

## Device verification still required

- Complete the direct HTTP Mofid OIDC/PKCE login on a real Android device and confirm the login HTML still exposes `__RequestVerificationToken` with the observed redirect sequence.
- Relaunch after a successful Mofid login and verify the biometric prompt in both cases: a still-valid access token and an expired token that triggers saved-credential API re-login after fingerprint.
- Test incorrect Mofid credentials and confirm the native UI shows the server/login failure without opening an embedded browser.
- If Mofid requests captcha, OTP, or another challenge, capture that network flow before implementing it; no guessed endpoint is included.
- Complete iPasargad captcha/login on a real Android device and confirm the observed `otauth-FU` cookie contract works outside the browser session.
- The iPasargad browser HAR already contained a `cookiesession1` WAF/session cookie whose creation was not captured; verify whether direct app login works without it before treating that login path as production-ready.
- Compare iPasargad Riton sell price/value against the live UI to confirm rial→toman conversion exactly once.
- Verify that the same ISIN connected through Mofid and iPasargad appears as two separate account-labelled cards.
- Confirm Mofid money/price unit mapping against the live EasyTrader UI.
- Start LAN sharing on one Android phone and connect from a second phone on the same Wi-Fi using the displayed host address and `viewer / portfolio123`.
- Confirm the generated debug APK installs and launches.
